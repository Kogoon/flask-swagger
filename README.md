# Docker + flask-swagger + MYSQL
`docker`, `mysql`, `ubuntu`, `python`, `flask`, `swagger`


### Instruction
**How to Use**
~~~
sudo docker-compose up -d --build
~~~

Test URL
~~~
https://{IP:5000}/api/doc
~~~

`port` `5000`
